version: "3"

volumes:
        mariadb:
                name: mariadb
                driver: local
                driver_opts:
                        device: ${LOCAL_USER}/data/mariadb
                        o: bind
                        type: none